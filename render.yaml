services:
  - type: web
    name: student-web-portal
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --worker-class gthread --workers 1 --threads 8 --bind 0.0.0.0:$PORT portal:app
    plan: free
    autoDeploy: true